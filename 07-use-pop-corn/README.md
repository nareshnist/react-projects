#   Part two
    - Don't use the fetch call in the render logic
    - Accessing the state imediatly after setiing , we can'tr get 
    - use Effect should have an async function as cb
    -  state lift up example with form
    - UseEffect Order effect.
    - API optimatization 